[package]
name = "schybrid"
version = "0.1.0"
edition = "2021"
authors = ["Shreepad Shukla"]
description = "Schedule hybrid work teams optimally while ensuring each team gets one day together in office"
repository = "https://github.com/shreepads/schybrid"
license = "AGPL-3.0-only"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "3.2.1", features = ["derive"] }
teams_info = { path = "../teams_info" }
teams_sched = { path = "../teams_sched" }